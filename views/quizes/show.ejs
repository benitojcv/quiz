    <form method="get" action="/quizes/<%= quiz.id %>/answer">
	Pregunta: <%= quiz.pregunta %> <p>
	<input type="text" name="respuesta" value="Responda aqu&iacute;" />
	<input type="submit" value="Enviar" />
    </form>
    <br/>
	<p><a href="/quizes/<%= quiz.id %>/comments/new"><button>Nuevo comentario</button></a></p>
	
	<p><strong>Comentarios:</strong></p>
	<% console.log("AAA: " + quiz.Comment);
		for (c in quiz.Comments) {%>
		<p>
		<form method="post" action="/quizes/<%= quiz.id %>/comments/<%= quiz.Comments[c].id %>/publish?_method=put">
		<% if (quiz.Comments[c].publicado || session.user) { %>
		- <%= quiz.Comments[c].texto %>
			<% if (session.user && !quiz.Comments[c].publicado) { %>
				<input type="submit" value="Publicar" />
			<% } %>
		<% } %>
		</form>
		</p>
	<% } %>
	